# PostgreSQL
POSTGRES_DB=train_booking
POSTGRES_USER=trainuser
POSTGRES_PASSWORD=change_me

# Backend
PORT=4000
# Fare in LKR per km (reserved coach)
RATE_PER_KM=2.5

# When running via docker-compose, keep this as-is.
VITE_API_URL=http://localhost:4000
